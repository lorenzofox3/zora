import {test} from '../../../dist/bundle/module.js';

test('tester 1', t => {
    t.ok(true, 'assert1');
    t.ok(true, 'assert2');
});

test('tester 2', t => {
    t.ok(true, 'assert3');
    t.ok(true, 'assert4');
});
